## install-host-packages
#
# Install packages on the host system to make the rest of this script
# run smoothly.

apt-get install -y ${host_packages[*]} \
    || die "Installing host packages failed!"
